{
  "name": "koka-respo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "build:koka": "node ./scripts/build-koka.mjs",
    "dev": "yarn build:koka && vite",
    "build": "yarn build:koka && tsc && vite build",
    "check": "yarn build:koka && tsc --noEmit",
    "test:koka": "node ./scripts/test-koka.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
